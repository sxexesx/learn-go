# ООП и ораганизация объектов в GO

## Какие принципы ООП реализованы в GO?

В языке GO реализованы все принципы ООП, но уникальным способом.

1. **Инкапсуляция** реализуется через строчные и заглавные буквы. Методы с заглавной буквы доступны вне пакета.

2. **Абстракция** достигается с помощью интерфейсов, который представляет набор сигнатур. Реализация интерфейсов происходит неявно.

3. **Полиморфизм**. Способность функции обрабатывать данные разных типов.

Go достигает полиморфизма через интерфейсы. Любой тип, реализующий интерфейс, может быть использован везде, где ожидается этот интерфейс

```golang
type Interface interface {
    Method(in string) (out string)
}

type Struct struct {}

func (s Struct) Method(in string) (out string) { ... }

type OtherStruct struct {}

func (s OtherStruct) Method(in string) (out string) { ... }

//------------------------//
func main() {
    var ex1 Interface = Struct{}
    var ex2 Interface = OtherStruct{}
    
    ex1.Mathod()
    ex2.Method()

}
```

4. **Наследование**. Go не поддерживает классическое наследование. Вместо этого язык использует композицию через встраивание структур (struct embedding)

```golang
type Hero struct {
    team string
}

func (h Hero) SayTeam() {
    fmt.Println("My Team is", h.team)
}

type Batman struct {
    Hero         // встраивание (embedding)
    name string
}

func (b Batman) SayImBatman() {
    fmt.Printf("I'm %s and I'm Batman\n", b.name)
}
```

## Что такое структура?

Составной тип данных, состоящий любых типов. Необъявленные поля заполняются дефолтными значениями.

## Какой размер у структуры?

Никакого

## Что такое интерфейс?

Программная синтаксическая структура, определяющая отношение между объектами, которые разделяют определенное поведенческое множество и не связаны никак иначе. 

```golang
type SomeInterface interface {
    SomeMethod(in string) (out string, err error)
}
```

## Зачем в go используются интерфейсы?

Для уменьшения связанности кода (decoupling)


## Какие есть свойства интерфейсов?

1. Интерфейс - это конкретная структура
```golang
type iface struct {
    tab *itab // <- ссылка на тип данных
    data unsafe.Pointer // <- ссылка на значение
}
```

2. Создать не nil интерфейс нельзя
3. За каждым не-nil интерфейсом стоит конкретный тип


## Best practice использования интерфейсов в GO

1. Описываются там, где используются
2. Должны содержать только то, что используется


## Какие интерфейсы стандартной библиотеки вы знаете?

пакет io (Read, Write, ..), error

## Какой размер у переменной типа interface?

Тут главное посмотреть на структуру и понять, что она состоит из **2х ссылок**. Т.о. размер пустого интерфейса равен размеру двух ссылок, т.е. 
```
4B * 2 = 8B  (x32)
или
8B * 2 = 16B (x64)
```

## Можем ли мы создать переменную типа interface? Может ли она быть равна nil?

Да, можем. Переменная типа interface будет равна nil, если обе ее ссылки равны nil, т.е. нет не только данных, но и информации о типах данных

```golang
func main() {
    var a int
    foo()
}

func foo(a interface{}) {
    println(a == nil) // false! появилась информация о типе 
}

```

## Дженерики или type parameters

Примеры [тут](/_notes/note_6/ex_6/)

```golang
func PrintSlice[T any](s []T) {
    for _, v := range s {
        fmt.Println(v)
    }
}
```

У дженериков могут быть констрейнты:
```golang
type MyConstraint interface {
	int16 | int32
}

func [T MyConstraint](t T) {...}

```

## Задачи
[Задача 1](note_6/ex_1/main.go)  
[Задача 2](note_6/ex_2/main.go)  
[Задача 3](note_6/ex_3/main.go)  
[Задача 4](note_6/ex_4/main.go)  
[Задача 5](note_6/ex_5/main.go)  




[>>> Назад <<<](../README.md)